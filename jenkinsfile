pipeline {
    agent any 

    stages {

        stage('checkout') {
            stage {

                sh '''
                  echo "this checkout stage"
                  '''
            }
        }
    }
}